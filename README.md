
# Simple Interest Calculator (Bash)

This project provides a Bash script `simple-interest.sh` to compute simple interest based on user input:
- Principal (amount)
- Rate of interest (annual %)
- Time period (years)

## Formula
Simple Interest = (Principal × Rate × Time) / 100
